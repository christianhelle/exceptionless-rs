name: Publish to crates.io

on:
  workflow_dispatch:
    inputs:
      release_tag:
        description: 'Release tag from Release Scaffolding (for example v0.1.0-42)'
        required: true
        type: string
      base_version:
        description: 'Base semver version (defaults to RELEASE_BASE_VERSION)'
        required: false
        type: string
      run_number_suffix:
        description: 'Optional run number suffix to match an existing release scaffold version'
        required: false
        type: string

permissions:
  contents: read

jobs:
  publish:
    name: Publish crate
    runs-on: ubuntu-latest
    environment: release
    env:
      DEFAULT_RELEASE_BASE_VERSION: 0.1.0
    steps:
      - name: Validate publish ref
        shell: bash
        run: |
          expected_ref="refs/heads/${{ github.event.repository.default_branch }}"
          if [ "${GITHUB_REF}" != "${expected_ref}" ]; then
            echo "::error::Publish workflow must run from ${expected_ref}"
            exit 1
          fi

      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.release_tag }}

      - name: Install stable Rust toolchain
        uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8

      - name: Cache Cargo artifacts
        uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32

      - name: Validate crates.io token
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        shell: bash
        run: |
          if [ -z "${CARGO_REGISTRY_TOKEN}" ]; then
            echo "::error::CARGO_REGISTRY_TOKEN secret is not configured for the release environment"
            exit 1
          fi

      - name: Resolve publish version
        id: version
        shell: bash
        run: |
          release_tag="${{ inputs.release_tag }}"
          base_version="${{ inputs.base_version }}"
          run_number_suffix="${{ inputs.run_number_suffix }}"

          if [[ ! "$release_tag" =~ ^v[0-9]+\.[0-9]+\.[0-9]+(-[0-9A-Za-z][0-9A-Za-z.-]*)?$ ]]; then
            echo "::error::release_tag must look like v0.2.0-42 or v0.2.0-rc.42"
            exit 1
          fi

          if [ -z "$base_version" ]; then
            base_version="${{ vars.RELEASE_BASE_VERSION }}"
          fi

          if [ -z "$base_version" ]; then
            base_version="${DEFAULT_RELEASE_BASE_VERSION}"
          fi

          if [[ ! "$base_version" =~ ^[0-9]+\.[0-9]+\.[0-9]+(-[0-9A-Za-z][0-9A-Za-z.-]*)?$ ]]; then
            echo "::error::Base version must look like 0.2.0 or 0.2.0-rc"
            exit 1
          fi

          if [ -z "$run_number_suffix" ]; then
            if [[ "$base_version" == *-* ]]; then
              suffix_prefix="v${base_version}."
            else
              suffix_prefix="v${base_version}-"
            fi

            if [[ "$release_tag" != ${suffix_prefix}* ]]; then
              echo "::error::release_tag ${release_tag} does not align with base_version ${base_version}"
              exit 1
            fi

            run_number_suffix="${release_tag#"$suffix_prefix"}"
          fi

          if [[ ! "$run_number_suffix" =~ ^[0-9]+$ ]]; then
            echo "::error::run_number_suffix must be numeric"
            exit 1
          fi

          if [[ "$base_version" == *-* ]]; then
            publish_version="${base_version}.${run_number_suffix}"
          else
            publish_version="${base_version}-${run_number_suffix}"
          fi

          if [ "$release_tag" != "v${publish_version}" ]; then
            echo "::error::release_tag ${release_tag} does not match computed version v${publish_version}"
            exit 1
          fi

          {
            echo "release_tag=${release_tag}"
            echo "base_version=${base_version}"
            echo "run_number_suffix=${run_number_suffix}"
            echo "publish_version=${publish_version}"
          } >> "$GITHUB_OUTPUT"

      - name: Apply publish version to Cargo.toml
        env:
          PUBLISH_VERSION: ${{ steps.version.outputs.publish_version }}
        run: |
          python - <<'PY'
          import os
          import re
          from pathlib import Path

          cargo_toml = Path("Cargo.toml")
          content = cargo_toml.read_text(encoding="utf-8")
          updated, count = re.subn(
              r'(?m)^version\s*=\s*"[^"]+"',
              f'version = "{os.environ["PUBLISH_VERSION"]}"',
              content,
              count=1,
          )

          if count != 1:
              raise SystemExit("Failed to update Cargo.toml version")

          cargo_toml.write_text(updated, encoding="utf-8")
          PY

      - name: Refresh Cargo.lock for publish version
        run: cargo generate-lockfile

      - name: Run publish validation
        run: cargo test

      - name: Dry-run crates.io publish
        run: cargo publish --dry-run --locked --allow-dirty

      - name: Publish crate to crates.io
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: cargo publish --locked --allow-dirty --no-verify

      - name: Summarize publish
        run: |
          {
            echo "## crates.io publish complete"
            echo
            echo "- Release tag: \`${{ steps.version.outputs.release_tag }}\`"
            echo "- Base version: \`${{ steps.version.outputs.base_version }}\`"
            echo "- Run number suffix: \`${{ steps.version.outputs.run_number_suffix }}\`"
            echo "- Published version: \`${{ steps.version.outputs.publish_version }}\`"
            echo "- Registry secret: \`CARGO_REGISTRY_TOKEN\` from the \`release\` environment"
          } >> "$GITHUB_STEP_SUMMARY"
