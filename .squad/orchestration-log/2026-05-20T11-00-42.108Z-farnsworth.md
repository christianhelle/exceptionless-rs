# Orchestration Log: Farnsworth (Integration Dev)

**Session:** 2026-05-20T13:00:42.108+02:00  
**Role:** Integration Dev  
**Status:** Single-feature semantics captured

## Assigned Task
Implement the user-directed single `opt-out` feature model while keeping the built-in HTTP surface available.

## Outcome
✅ **Completed:** Established the semantic shape that leaves `ExceptionlessClient::with_api_key(...)` and `transport::http::HttpTransport` available in every build, makes `reqwest` unconditional again, and short-circuits `send()` and `submit_batch()` to synthetic accepted success when `opt-out` is enabled.

## Confirmed
- the semantic slice stayed narrowly on feature behavior rather than transport API redesign
- Amy's first review rejected the initial revision for doc truthfulness and missing direct `submit_batch()` proof
- revision ownership moved to Bender, so Farnsworth's semantic slice remains uncommitted in the worktree
